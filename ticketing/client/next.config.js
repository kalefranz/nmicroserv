// helps with next.js not always updating automatically in the browser

// // Section 215. Seems like we shouldn't be doing this
// module.export = {
//   webpackDevMiddlewaire: config => {
//     config.watchOptions.poll = 300;
//     return config;
//   }
// };
